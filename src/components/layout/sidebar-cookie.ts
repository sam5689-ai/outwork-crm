// Kept out of the "use client" sidebar module so the server layout can read
// the real string (importing a value from a client module yields a reference).
export const SIDEBAR_COOKIE = "sidebar-collapsed";
